The Full-Stack Replit PromptRole: Act as an expert Full-Stack Developer. Your task is to build a "Financial Sandbox & What-If Analyzer" web application.Core Concept: This is a high-level, one-off scenario planning tool designed to forecast wealth and cash flow over time based on current assets, monthly/yearly inflows/outflows, and investment growth rates.Tech Stack Constraints: > * Frontend: React, Tailwind CSS for modern styling, and Recharts (or similar) for data visualization.Backend: Python with FastAPI.Communication: Use fetch or axios on the frontend. Crucial: Ensure CORS middleware is properly configured in FastAPI so the frontend can successfully communicate with the backend.Backend Architecture (FastAPI):Build RESTful endpoints to manage the user's financial data (e.g., /api/budget).Instead of a full database for now, use a lightweight in-memory Python dictionary or write to a local .json file to store the user's saved data and variables between sessions.The backend should serve as the source of truth for the baseline financial data (Income, Expenses, Assets, Rolling Investments).Frontend Data Model & Inputs:Build an intuitive UI to add/remove/edit the following categories. All items need a "Monthly" or "Yearly" toggle:Income: Salary, side hustles, etc.Expenses: Fixed (rent) and Variable (travel, shopping).Existing Assets: Cash deposits, current investment portfolio balances.Rolling Investments: Monthly/Yearly contributions to investments.Financial Mathematics & Default Logic:The app must calculate compound growth accurately. Use these default formulas in your logic:Existing Investments: $FV = PV(1 + r)^t$ (where PV is present value, r is annual rate, t is years).Rolling Monthly Investments: $FV = PMT \times \frac{(1 + \frac{r}{12})^{12t} - 1}{\frac{r}{12}}$ (where PMT is monthly contribution).Core Features & "What-If" Engine:The Projection Engine: Calculate and project the user's total net worth, cash flow, and investment growth over time periods (1, 5, and 10+ years).Global "What-If" Sliders: Create a dedicated "Scenario" panel with sliders that apply global modifiers to the baseline data (e.g., "Increase all expenses by X%", "Adjust average investment return to Y%").Advanced Formula Settings: Expose the compounding frequency (e.g., switch from Annual to Monthly) and allow the user to tweak the default inflation rate or return formulas directly from the UI.Instant Recalculation: Adjusting any base value, "What-If" slider, or advanced formula setting must instantly update all charts on the frontend.UI & Visualizations:Dashboard Layout: A clean layout where inputs/sliders are on the left, and visual results are on the right.Charts: >     * A Donut Chart showing the breakdown of Income vs. Expenses vs. Investments.A Stacked Bar Chart or Area Chart showing projected wealth growth over time (separated by Cash vs. Investments).Timeframe Toggle: A simple switch to view all baseline metrics in "Per Month" or "Per Annum" formats.